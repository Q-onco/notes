# Session Log — 2026-06-04 · Session 1

## Project: Q-onco/notes
## Summary
VPS migration setup — app now running in parallel at q-onco.phytolabs.in alongside the existing GitHub Pages deployment. Repo stays public; both deployments live simultaneously.

---

## What Was Done

### Deep App Audit (pre-migration)
Full scan of all source files to identify every hardcoded URL and path dependency before touching anything. Findings:
- Only 2 source files needed path changes: `vite.config.ts` (base path) and `public/manifest.json` (PWA scope/start_url)
- Worker `wrangler.toml` needed CORS ALLOWED_ORIGIN updated
- `github.ts`, `crypto.ts`, `store.svelte.ts`, `idb.ts`, `groq.ts` — zero changes needed (all domain-agnostic)
- Encryption (AES-256-GCM, PBKDF2 600k iterations, static salt) untouched — existing .enc files remain readable

### VPS Infrastructure
- Added `q-onco.phytolabs.in` domain to Hestia (creates nginx + Apache config + web root at `/home/shiva/web/q-onco.phytolabs.in/public_html/`)
- Wrote `.htaccess` for SPA routing (all non-file paths → `index.html`)
- Generated fresh SSH ed25519 keypair at `/root/.ssh/qonco_deploy` for GitHub Actions → VPS deploy
- Added public key to `/home/shiva/.ssh/authorized_keys`

### Code Changes
- `app/vite.config.ts`: `base: '/notes/'` → `base: process.env.BASE_PATH ?? '/notes/'`
  - GitHub Pages workflow (unchanged) builds with default `/notes/` — GH Pages unaffected
  - New VPS workflow sets `BASE_PATH=/` — builds with root base path
- `worker/qonco-worker.js` line 111–112: changed `ALLOWED_ORIGIN` handling from single string to comma-separated list
  - Both `https://q-onco.github.io` and `https://q-onco.phytolabs.in` now accepted
  - Logic: check if request Origin is in the allowed list; if yes, echo it back; else use first entry
- `worker/wrangler.toml`: `ALLOWED_ORIGIN = "https://q-onco.github.io,https://q-onco.phytolabs.in"`

### New Workflow: `.github/workflows/deploy-vps.yml`
- Triggers on push to main (runs alongside existing deploy.yml)
- Builds with `BASE_PATH=/`
- Patches `dist/manifest.json` scope/start_url from `/notes/` to `/`
- Rsyncs `app/dist/` to `/home/shiva/web/q-onco.phytolabs.in/public_html/` via SSH
- Excludes `.htaccess` from rsync (preserves SPA routing)

### GitHub Actions Secrets (set via gh CLI)
- `VPS_HOST` = VPS_IP_REDACTED
- `VPS_USER` = shiva
- `VPS_SSH_KEY` = private key content of qonco_deploy ed25519 pair

### Initial Manual Build + Deploy
- Built app locally on VPS with `BASE_PATH=/ npm run build` (39.81s, 125 modules)
- Patched `dist/manifest.json` manually
- Rsynced to web root — app files live

### Stashed in-progress work
- Local uncommitted changes (streamCompareAnalysis, streamNodeAnalysis, pure white light mode, plan-2 completion) safely stashed: `stash@{0}`
- Will be popped and committed separately in next session

---

## Pending (requires user action)

### DNS — GoDaddy A record
Add this record in GoDaddy DNS for `phytolabs.in`:
```
Type: A
Name: q-onco
Value: VPS_IP_REDACTED
TTL: 600
```
GoDaddy API key was not found in VPS environment — needs to be added manually in GoDaddy UI (takes ~2 minutes).

### SSL — After DNS propagates
Once `q-onco.phytolabs.in` resolves to the VPS, run:
```bash
/usr/local/hestia/bin/v-add-letsencrypt-domain shiva q-onco.phytolabs.in
```

### Worker Deploy — After this commit is pushed
```bash
cd /root/notes/worker && npm run deploy
```
(Requires Cloudflare API token in environment — deploy from local machine if not available on VPS)

### Verification Steps (after DNS + SSL)
1. Open https://q-onco.phytolabs.in
2. Login with GitHub PAT
3. Confirm all .enc data loads (notes, journal, tasks, chat, research, grants etc.)
4. Test Enzo AI (Settings → Test Worker URL)
5. Test file upload (R2)
6. Test audio recording
7. Verify PWA installable (manifest scope = /)
8. Check GitHub Pages still works at q-onco.github.io/notes/

---

## Files Changed
- `app/vite.config.ts` — base path env-driven
- `worker/qonco-worker.js` — multi-origin CORS
- `worker/wrangler.toml` — ALLOWED_ORIGIN both domains
- `.github/workflows/deploy-vps.yml` — NEW: SSH rsync deploy to VPS
- `logs/2026-06-04_session-1.md` — this file
