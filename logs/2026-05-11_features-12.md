# Session log — 2026-05-11 (12 features batch)

## Commit
`da42248` feat: 12 new features — notes, research, and files

## Features implemented

### Notes (4)
1. **Drag-to-reorder blocks** — `GlobalDragHandle` ProseMirror extension in `tiptap-extensions.ts`. Floating `⠿` handle appears on hover, drag to reorder top-level ProseMirror nodes. Added to `RichEditor.svelte` extensions array.
2. **Mermaid dark theme** — `tiptap-extensions.ts` MermaidBlock node view reads `document.documentElement.getAttribute('data-theme')` before each render, passes `theme: 'dark'` when active.
3. **Word-count target** — `wordTarget?: number` on `Note` type. Progress bar in Editor footer with green ≥100%, blue ≥60%, yellow <60%. Inline goal setter (pencil button) saves on Enter/blur.
4. **Expanded keyboard shortcuts in Help** — Three new groups: Notes — Editor, Notes — Toolbar, Notes — Bubble menu.

### Research (4)
5. **Paper collections** — `PaperCollection` type + `paperCollections` state in store. Collections bar with create/delete/color-picker. Papers assigned via select; filter by clicking pill. Saved to research `.enc` file.
6. **Enzo paper compare** — `comparePapers()` in `groq.ts` (Llama 70B). Select 2–3 reading list items via checkboxes, compare bar appears, streaming markdown table + synthesis in `compare-result` card.
7. **Related papers (OpenAlex)** — `fetchRelated()` 2-step: DOI → work ID → citing papers. Inline panel per reading list item, with Open/+List/Pin per result.
8. **3-state reading progress** — `readStatus?: 'unread' | 'in-progress' | 'done'` on `ReadingListItem`. Status button cycles states; `read` boolean kept in sync. Done items are visually greyed.

### Files (4)
9. **Rename file** — Pencil icon appears on hover in detail panel title row. Inline input, Enter/blur saves, Escape cancels.
10. **Create note from PDF** — "Note" button in detail actions for open PDFs. Calls `extractPdfText()`, creates `Note` with body = extracted text, links file to note, navigates to note.
11. **Bulk select + actions** — Checkbox icon in topbar toggles bulk mode. Checkboxes on each file item. Bulk action bar: move-to-folder select + delete button.
12. **Image gallery + lightbox** — Gallery icon in topbar (shown when images present). Responsive thumbnail grid. Click thumbnail → full-screen lightbox with prev/next navigation.

## Files changed
- `app/src/lib/types.ts` — `wordTarget` on Note, `collectionIds` on PaperResult, `PaperCollection` interface, `readStatus` on ReadingListItem
- `app/src/lib/store.svelte.ts` — `paperCollections` state, extend research save/load
- `app/src/lib/groq.ts` — `comparePapers()` function
- `app/src/lib/tiptap-extensions.ts` — `GlobalDragHandle` extension, Mermaid dark theme fix
- `app/src/components/RichEditor.svelte` — `GlobalDragHandle` in extensions, CSS for `.pm-drag-handle`
- `app/src/components/Editor.svelte` — word-count goal bar + setter in footer
- `app/src/components/Research.svelte` — all 4 research features + CSS; `marked` import added
- `app/src/components/Files.svelte` — all 4 files features + CSS; `Note` type import added
- `app/src/components/Help.svelte` — 8 new What's New cards, updated Research/Files module descriptions, 2 new shortcut groups

## Build
`✓ built in 32.28s` — no errors
