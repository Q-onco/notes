# Plan: Compare analysis streaming + node panels + pure white light mode
Date: 2026-05-27

## Goals
- 500-word streaming comparison analysis from Enzo (below graph)
- Node click → 250-word concept analysis in a sliding side panel (click IS the trigger)
- Pure white light mode (replace beige #faf8f5 with #ffffff, GitHub-style palette)
- Light-mode CSS overrides for compare modal chrome (graph SVG stays dark)
- Update Help.svelte with new features

## Changes

### groq.ts
- [ ] Add `streamCompareAnalysis(papers, graph, onChunk, signal?)` — 500-word narrative
- [ ] Add `streamNodeAnalysis(node, papers, onChunk, signal?)` — 250-word concept analysis

### app.css
- [ ] Replace beige light theme with pure white / GitHub-style colours
  - --bg: #faf8f5 → #ffffff
  - --sf2: #f5f2ed → #f6f8fa
  - --sf3: #ede9e2 → #eaeef2
  - --bd: #ddd9d0 → #d0d7de
  - --bd2: #c8c3b8 → #b4bac0
  - --tx: #1c1a18 → #1f2328
  - --tx2: #4a4640 → #59636e
  - --mu: #8a8480 → #848d97
  - --ac: #3a5fc8 → #0969da
  - --ac-h: #2d4db0 → #0860ca
  - --ac-bg: #eef2fc → #ddf4ff
  - --scrollbar: #ddd9d0 → #d0d7de

### Research.svelte — script
- [ ] Add `streamCompareAnalysis`, `streamNodeAnalysis` to import
- [ ] Add state: `compareAnalysisText`, `compareAnalysisLoading`, `compareAnalysisAbort`
- [ ] Add state: `nodeAnalysisText`, `nodeAnalysisLoading`, `nodeAnalysisAbort`, `nodeAnalysisPanelNode`
- [ ] Add state: `comparePapersFullData` (store full paper objects for re-use in node analysis)
- [ ] Update `runCompare()`: store papersData in state, then stream analysis after graph loads
- [ ] Add `handleNodeAnalysis(node)` — explicit trigger, no $effect
- [ ] Update node click in `handleSVGMouseUp` to call `handleNodeAnalysis` for concept nodes

### Research.svelte — template
- [ ] Replace verdict bar with streaming analysis section (verdict + expandable deep analysis)
- [ ] Add node detail panel (absolute overlay inside cmp-graph-wrap, right side ~300px)
  - Shows node type badge, label, description
  - Streams 250-word analysis
  - Abort button while loading, close button

### Research.svelte — CSS
- [ ] Styles for streaming analysis section
- [ ] Styles for node detail panel (dark semi-transparent overlay, slides in)
- [ ] [data-theme="light"] overrides for modal chrome (header, pills, verdict, table)
  - Graph SVG canvas remains dark (intentional — it's a data viz)

### Help.svelte
- [ ] Update Research: Enzo Paper Compare entry to describe knowledge graph features

## Verification
- [ ] Build passes (npm run build)
- [ ] Light mode looks clean white (not beige)
- [ ] Compare analysis streams after graph renders
- [ ] Node click opens panel + streams 250 words
- [ ] Node deselect / close clears panel
- [ ] Abort works for both streams
- [ ] Modal chrome adapts in light mode
