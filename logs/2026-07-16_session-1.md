# Session 2026-07-16 (plaintext — QONCO_KEY not available, encryption skipped)

## Context
Working on the VPS (dev.phytoevolution.in / 51.83.192.49) on unrelated security hardening:
moved sshd from port 22 to port 22422 to cut brute-force scanner traffic, then closed port 22
entirely (confirmed via connection-refused test) after verifying the new port works from the
user's own machine.

## Change in this repo
`.github/workflows/deploy-vps.yml` used `appleboy/ssh-action@v1.0.3` with no explicit `port`,
which defaults to 22 — since port 22 is now closed on the VPS, the next push to `main` would
have failed to deploy. Added `port: "22422"` to the ssh-action step. Commit c96eb41.

## Verification
- Confirmed sshd listening only on 22422 (`ss -tlnp`)
- Confirmed port 22 refuses connections (`/dev/tcp` test)
- Did not test-run this workflow end-to-end (no push since the fix) — worth watching the next
  deploy to confirm the port change actually works for appleboy/ssh-action, not just for
  interactive `ssh -p`.
