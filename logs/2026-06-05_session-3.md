# Session log — 2026-06-05 session 3

## Summary
Diagnosed and partially resolved Enzo breakage. Full VPS/GitHub rewiring still pending.

## Root cause of Enzo breakage
June 4th evening session added markdown rendering (marked v14 + markedHighlight +
highlight.js) to Enzo.svelte. The `markedHighlight` integration was implemented
carelessly — called at Svelte module level, `hljs.highlight` called with
`'plaintext'` language not registered, causing `ReferenceError: text is not defined`
at runtime. This is an implementation error, not a bug in the libraries themselves.

## What was rolled back
Code restored to commit 5dc1a16 (June 4th 19:56 IST — last known-good state).
All .enc data files preserved at current state.

**Features lost in rollback (to be re-implemented correctly):**
1. New chat button + raised context limits
2. File context — Enzo auto-reads files mentioned by name
3. Resume past sessions, session lookup by ID
4. History ×30, copy button, regenerate button, file persistence
5. Markdown rendering with safe implementation
6. Loading indicator, multi-file context (up to 3 files)

## Today's deployment mess (caused by session)
- notes-deploy and notes share the same Q-onco/notes remote
- Every push from notes-deploy triggered GitHub Pages redeploy
- Multiple bad builds deployed to both VPS and GitHub Pages
- /root/notes (dev clone) is now ~20 commits behind remote main
- Repo was made public (was private since 2026-06-04)

## Current state
- VPS (q-onco.phytolabs.in): working, rolled-back Enzo ✓
- GitHub Pages (q-onco.github.io/notes): working, same rolled-back code ✓
- Repo: public
- 6 Enzo feature commits pending correct re-implementation

## Decisions deferred (discuss next session)
1. Make repo fully private (VPS-only deployment, disable GitHub Pages workflow)
   - Requires verifying data PAT has full `repo` scope (not just public_repo)
   - Private repo + public PAT scope = silent read/write failures (likely cause of
     20s load time and chat-not-saving when repo was briefly private today)
2. Re-implement rolled-back Enzo features correctly, one at a time
3. Sync /root/notes dev clone with remote main
4. Broader VPS rewiring discussion (static vs dynamic site architecture)

## Key rule for next session
Do NOT mix notes and notes-deploy push targets without checking first.
Both point to Q-onco/notes remote — pushing either one triggers both workflows.
