# Session log — 2026-05-03 09:27

## Summary
Three sessions rolled into one push: task-row overflow fix, Enzo status wave, RULES.md, mobile baseline; then export on all pages + pinned research papers dashboard.

---

## Full conversation

**User (batch 1):** task-row still broken, add rule for iPhone/iPad/MacBook + PWA compat, enzo status cramped + too fast + needs wave

**User (batch 2):** export button on all pages; research articles pin to dashboard and persist until removed

---

## Files changed

### RULES.md (new — repo root)
7 dev rules. Rule 5: device compat table (iPhone/iPad/MacBook). Rule 6: PWA integrity checklist. Rule 7: Enzo character.

### Dashboard.svelte
- min-width:0 + overflow:hidden on .dash-card and row containers — finally contains flex children in grid cells
- Pinned papers section: appears when pinnedPapers.length > 0, shows source badge + linked title + journal + year, unpin × per item, Export button
- Import of exportPapers from lib/export

### Enzo.svelte
- IDLE_STATUSES trimmed 15 → 8 meaningful phrases
- Interval slowed 4500ms → 9000ms
- HTML restructured: enzo-name-stack (column) wraps name + status — status now on its own line, no longer cramped
- status-wave keyframe: fades to 0.1 at 83% then recovers — slow, gentle, one wave per cycle
- thinking-pulse: slow 2.4s breathe when streaming

### Shell.svelte
- $effect: closes sidebar + enzo on window.innerWidth ≤ 540 (mobile first open)
- CSS: @media ≤540px sidebar becomes position:absolute overlay (doesn't squish content)
- Research now receives {showToast} prop

### app.css
- @media ≤540px: btn-icon min 40×40px, btn min-height 40px

### Weather.svelte
- city-desc hidden at ≤540px

### lib/export.ts (new)
exportNote, exportAllNotes, exportJournal, exportTasks, exportPapers — all download .md files

### lib/github.ts
Added 'data/pinned.enc' to PATHS

### lib/store.svelte.ts
- pinnedPapers state + sha
- savePinnedPapers(), isPinned(), pinPaper(), unpinPaper()
- loadAll now fetches pinned papers

### Research.svelte
- showToast prop added
- Pin button (bookmark) on each paper, filled = pinned; togglePin()
- Export pinned button in header
- sendToEnzo: removed store.view='enzo' bug

### Editor.svelte
- Export note button in toolbar → .md download

### Journal.svelte
- Export button in header → all entries .md

### Tasks.svelte
- Export button in header → open/done checklist .md
