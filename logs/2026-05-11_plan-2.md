# Session plan — 2026-05-11 (plan-2)
# Presentations overhaul + Files overhaul + Slidev-inspired features
# 26 items across 4 groups. Tick each box as it lands. No push with silent gaps.

---

## Group A — Presentations bug fixes (P1–P2)

- [ ] **P1a** Fix slide editing — add `user-select: text; -webkit-user-select: text` to `.slide-content-wrap` in Presentations.svelte (CSS user-select from `draggable="true"` on card suppresses TipTap cursor)
- [ ] **P1b** Fix slide editing — guard RichEditor `$effect` value sync with `if (editor.isFocused) return` in RichEditor.svelte (prevents setContent loop on every keystroke)
- [ ] **P2** Persist AI context — add `aiContext?: { brief: string; outline: string; concepts: string; slideTitles: string[] }` to `Presentation` in types.ts; save after generateSlidesDeck completes; display saved layers in source sidebar when revisiting a presentation (with "last generated" label)

---

## Group B — Files overhaul (F1–F4)

- [ ] **F1** Files performance — on loadAll(), strip `data` field from FileRecord objects that also have an `r2Key` (legacy base64 blobs bloating files.enc in memory)
- [ ] **F2** Files performance — IndexedDB metadata cache: cache decrypted files array keyed by files.enc SHA; show cached list instantly on navigation; refresh in background when SHA changes
- [ ] **F3** Files cross-module linking — extend FileRecord with `linkedPresentationIds: string[]`, `linkedPaperIds: string[]`, `linkedJournalIds: string[]`, `linkedTaskIds: string[]`, `linkedGrantIds: string[]`, `linkedManuscriptIds: string[]`; bidirectional link UI in file detail panel; store save includes new fields
- [ ] **F4a** Files — sort controls (name / size / date / type) in topbar
- [ ] **F4b** Files — inline thumbnail preview for images in list + grid view
- [ ] **F4c** Files — star/favourite flag (`starred: boolean` on FileRecord, star button in list, starred filter chip)
- [ ] **F4d** Files — folder colour labels (`folderColor?: string` on FileRecord per folder, colour dot picker in folder row)
- [ ] **F4e** Files — file usage summary inline ("used in N notes, M presentations…")
- [ ] **F4f** Files — quick-look hover preview (350ms delay, floating panel, same pattern as Sidebar note hover)
- [ ] **F4g** Files — duplicate file (clone FileRecord with new id, same r2Key)
- [ ] **F4h** Files — file version history (track upload timestamps as `versions: {uploadedAt: number, size: number}[]` on FileRecord)
- [ ] **F4i** Files — upload progress bar (replace boolean `uploading` with 0–100 progress value via XHR or fetch stream)
- [ ] **F4j** Files — paste-to-upload: Ctrl+V on files panel pastes clipboard image → uploads to R2
- [ ] **F4k** Files — recent files widget on Dashboard (last 5 opened files)
- [ ] **F4l** Files — note-from-PDF template picker: when creating note from PDF, offer 3 templates (Paper notes / Methods extraction / Data log) instead of raw text dump

---

## Group C — Presentations: Slidev Tier 1 (S1–S9)

- [ ] **S1** Slide layouts — add `layout` field to Slide type; 12 presets: cover, section, two-cols, two-cols-header, image-left, image-right, full-image, stat-callout, quote, statement, end, default; layout picker icon row in slide card header; each layout pre-scaffolds the HTML written into the RichEditor; Enzo generation unchanged
- [ ] **S2** KaTeX math in slides — add InlineMathExtension + BlockMathExtension from tiptap-extensions.ts to the slide RichEditor extensions array
- [ ] **S3** Mermaid in slides — add MermaidBlockExtension from tiptap-extensions.ts to the slide RichEditor
- [ ] **S4** Step-reveal (v-click equivalent) — `revealBullets: boolean` on Slide; toggle button in slide card header; in present mode `<li>` items start hidden, arrow-right advances one step before moving to next slide; step counter shown in presenter HUD
- [ ] **S5** Slide transitions — `transition` field on Slide (and global default on Presentation); 6 presets: none, fade, slide-left, slide-right, slide-up, slide-down; picker in slide card header; CSS transition applied in present mode
- [ ] **S6** Presenter mode upgrade — replace basic fullscreen with dual-panel view: current slide (large, left) + next slide preview + speaker notes + timer + step counter (right panel); keyboard: → next, ← prev, O overview, G goto, D dark toggle, F fullscreen, Esc exit
- [ ] **S7** Quick Overview panel (O key) — fullscreen thumbnail grid overlay, all slides visible, click to jump; works in and out of present mode
- [ ] **S8** Speaker notes upgrade — replace `<input>` with `<textarea>`; add Notes batch editor view (all slides + notes in one scrollable editor, separated by slide markers)
- [ ] **S9** PDF export — print-CSS approach: print window with slides laid out as @page blocks at 16:9; `window.print()` trigger; speaker notes as @media print section (optional toggle)

---

## Group D — Presentations: Slidev Tiers 2 + 3 (S10–S20)

- [ ] **S10** Drawing/annotation in present mode — `<canvas>` overlay; tools: pen (freehand), highlighter (semi-transparent thick), eraser; 5 colour presets; clear-all button; ephemeral (not saved)
- [ ] **S11** Per-slide background — `background` field on Slide (hex colour, gradient string, or image URL); colour/gradient picker in slide card header; applied in editor + present view
- [ ] **S12** Camera view — `<video>` overlay in present mode from `getUserMedia`; drag to reposition; corner handle to resize; toggle in presenter HUD; size/position saved to localStorage
- [ ] **S13** PNG export per slide — html2canvas renders each slide; downloads as ZIP; also enables "copy slide as image" clipboard action
- [ ] **S14** PPTX export — pptxgenjs; each slide html2canvas'd → image embedded in PPTX slide; speaker notes injected as PPTX notes
- [ ] **S15** Remote control — QR code in present mode encoding `/remote?id={presId}`; worker adds `GET /remote/events` SSE + `POST /remote/cmd`; phone page: prev/next/laser buttons
- [ ] **S16** Goto dialog (G key) — small input overlay "Go to slide N of M"; Enter jumps; works in present + overview
- [ ] **S17** Wake Lock — `navigator.wakeLock.request('screen')` on entering present mode; release on exit
- [ ] **S18** Hide idle cursor in present mode — `cursor: none` after 3s no `mousemove`; restore on movement
- [ ] **S19** Directional transitions — `transitionBack` field on Slide; forward and backward navigation use different transition presets
- [ ] **S20** CSS filters panel — per-presentation filter sliders: brightness, contrast, saturation, hue-rotate; applied as CSS `filter:` on slide wrapper in present + export

---

## End-of-session checklist

- [ ] Build passes (`npm run build` in app/) with zero errors
- [ ] Help.svelte updated with What's New cards for all completed features
- [ ] Session log written at logs/2026-05-11_session-presentations-files.md
- [ ] All ticked boxes verified against actual code (no wishful ticking)
- [ ] Plan committed alongside code
