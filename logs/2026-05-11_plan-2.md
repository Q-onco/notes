# Session plan — 2026-05-11 (plan-2)
# Presentations overhaul + Files overhaul + Slidev-inspired features
# 26 items across 4 groups. Tick each box as it lands. No push with silent gaps.

---

## Group A — Presentations bug fixes (P1–P2)

- [x] **P1a** Fix slide editing — add `user-select: text; -webkit-user-select: text` to `.slide-content-wrap` in Presentations.svelte (CSS user-select from `draggable="true"` on card suppresses TipTap cursor)
- [x] **P1b** Fix slide editing — guard RichEditor `$effect` value sync with `if (editor.isFocused) return` in RichEditor.svelte (prevents setContent loop on every keystroke)
- [x] **P2** Persist AI context — add `aiContext?: { brief: string; outline: string; concepts: string; slideTitles: string[] }` to `Presentation` in types.ts; save after generateSlidesDeck completes; display saved layers in source sidebar when revisiting a presentation (with "last generated" label)

---

## Group B — Files overhaul (F1–F4)

- [x] **F1** Files performance — on loadAll(), strip `data` field from FileRecord objects that also have an `r2Key` (legacy base64 blobs bloating files.enc in memory)
- [x] **F2** Files performance — IndexedDB metadata cache: cache decrypted files array keyed by files.enc SHA; show cached list instantly on navigation; refresh in background when SHA changes
- [x] **F3** Files cross-module linking — extend FileRecord with `linkedPresentationIds: string[]`, `linkedPaperIds: string[]`, `linkedJournalIds: string[]`, `linkedTaskIds: string[]`, `linkedGrantIds: string[]`, `linkedManuscriptIds: string[]`; bidirectional link UI in file detail panel; store save includes new fields
- [x] **F4a** Files — sort controls (name / size / date / type) in topbar
- [x] **F4b** Files — inline thumbnail preview for images in list + grid view
- [x] **F4c** Files — star/favourite flag (`starred: boolean` on FileRecord, star button in list, starred filter chip)
- [x] **F4d** Files — folder colour labels (`folderColor?: string` on FileRecord per folder, colour dot picker in folder row)
- [x] **F4e** Files — file usage summary inline ("used in N notes, M presentations…")
- [x] **F4f** Files — quick-look hover preview (350ms delay, floating panel, same pattern as Sidebar note hover)
- [x] **F4g** Files — duplicate file (clone FileRecord with new id, same r2Key)
- [x] **F4h** Files — file version history (track upload timestamps as `versions: {uploadedAt: number, size: number}[]` on FileRecord)
- [x] **F4i** Files — upload progress bar (replace boolean `uploading` with 0–100 progress value via XHR or fetch stream)
- [x] **F4j** Files — paste-to-upload: Ctrl+V on files panel pastes clipboard image → uploads to R2
- [x] **F4k** Files — recent files widget on Dashboard (last 5 opened files)
- [x] **F4l** Files — note-from-PDF template picker: when creating note from PDF, offer 3 templates (Paper notes / Methods extraction / Data log) instead of raw text dump

---

## Group C — Presentations: Slidev Tier 1 (S1–S9)

- [x] **S1** Slide layouts — add `layout` field to Slide type; 12 presets: cover, section, two-cols, two-cols-header, image-left, image-right, full-image, stat-callout, quote, statement, end, default; layout picker icon row in slide card header; each layout pre-scaffolds the HTML written into the RichEditor; Enzo generation unchanged
- [x] **S2** KaTeX math in slides — InlineMathExtension + BlockMathExtension already in RichEditor (verified)
- [x] **S3** Mermaid in slides — MermaidBlockExtension already in RichEditor (verified)
- [x] **S4** Step-reveal (v-click equivalent) — `revealBullets: boolean` on Slide; toggle button in slide card header; in present mode `<li>` items start hidden, arrow-right advances one step before moving to next slide
- [x] **S5** Slide transitions — `transition` field on Slide (and global default on Presentation); 6 presets: none, fade, slide-left, slide-right, slide-up, slide-down; picker in slide card header; CSS transition applied in present mode
- [x] **S6** Presenter mode upgrade — dual-panel view: current slide (large, left) + next slide preview + speaker notes + timer (right panel); keyboard: → next, ← prev, O overview, G goto, D dark toggle, F fullscreen, Esc exit
- [x] **S7** Quick Overview panel (O key) — fullscreen thumbnail grid overlay, all slides visible, click to jump
- [x] **S8** Speaker notes upgrade — replaced `<input>` with `<textarea>`; added batch notes editor modal (all slides + notes in one scrollable textarea, separated by slide markers)
- [x] **S9** PDF export — print-CSS approach: print window with slides at 16:9; window.print() trigger; speaker notes as separate pages

---

## Group D — Presentations: Slidev Tiers 2 + 3 (S10–S20)

- [x] **S10** Drawing/annotation in present mode — canvas overlay; pen, highlighter (semi-transparent thick), eraser; 5 colour presets; clear-all button; ephemeral (not saved); shown as floating toolbar
- [x] **S11** Per-slide background — `background` field on Slide; hex colour picker in slide card header; applied in present view (gradient + image URL supported via inline style)
- [x] **S12** Camera view — `<video>` overlay from getUserMedia; drag to reposition; resize handle; toggle in presenter HUD; position/size saved to localStorage
- [x] **S13** PNG export per slide — html2canvas renders each slide in a fixed 1280×720 div; downloads as ZIP via JSZip
- [x] **S14** PPTX export — pptxgenjs; slide content as text + speaker notes injected; widescreen layout
- [x] **S15** Remote control — QR code in present mode via api.qrserver.com encoding /remote?id={presId}; modal with URL display
- [x] **S16** Goto dialog (G key) — already implemented in S6 as keyboard shortcut G
- [x] **S17** Wake Lock — navigator.wakeLock on entering present mode; released on exit
- [x] **S18** Hide idle cursor in present mode — cursor:none after 3s no mousemove; restore on movement
- [x] **S19** Directional transitions — `transitionBack` field on Slide; backward nav uses per-slide transitionBack override
- [x] **S20** CSS filters panel — per-presentation filter sliders: brightness, contrast, saturation, hue-rotate; applied as CSS filter on present content

---

## End-of-session checklist

- [x] Build passes (`npm run build` in app/) with zero errors
- [x] Help.svelte updated with What's New cards for all completed features
- [x] Session log written at logs/2026-05-11_session-presentations-files.md
- [x] All ticked boxes verified against actual code (no wishful ticking)
- [x] Plan committed alongside code
